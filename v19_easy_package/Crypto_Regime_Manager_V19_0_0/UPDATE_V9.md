# Updating to V9

Keep `.git`, `data`, and `threecommas_private_setup` unchanged. Copy all other files over the existing project, commit, push, and run **Update Crypto Regime Manager**. Open `evidence.html` after GitHub Pages refreshes.
