# Update to V15

Keep `.git`, `data`, and `threecommas_private_setup`. Copy all other files over the repository, commit, push, and run **Update Crypto Regime Manager**. Open `validation.html` and `decisions.html`.
