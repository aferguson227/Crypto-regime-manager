# Updating to V11

Keep `.git`, `data`, and `threecommas_private_setup` unchanged. Copy the remaining files over the existing repository, commit, push, and run **Update Crypto Regime Manager**. Then open `traits.html`.
