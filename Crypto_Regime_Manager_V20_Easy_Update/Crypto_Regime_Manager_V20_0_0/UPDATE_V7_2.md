# Update to V7.2

1. Fetch and pull the current repository in GitHub Desktop.
2. Preserve `.git`, `data`, and `threecommas_private_setup`.
3. Copy all V7.2 files except the packaged `data` folder into the repository and replace existing files.
4. Commit with `Add ranked SUI hypothesis engine V7.2` and push.
5. Run `Update Crypto Regime Manager`.
6. Open `research.html`.

No API secrets or RSA keys need to be recreated.
