# Update to V7.1

1. In GitHub Desktop, Fetch origin and Pull origin.
2. Preserve `.git`, `data`, and `threecommas_private_setup`.
3. Copy the V7.1 package contents into the repository, replacing code and documentation files.
4. Do not overwrite the `data` folder.
5. Commit with `Add automatic SUI Candidate B experiments V7.1`.
6. Push origin.
7. Run `Update Crypto Regime Manager` in GitHub Actions.
8. Open `research.html` after GitHub Pages updates.

No API secrets, RSA keys, TEL settings, TAO settings, or candle files need to be recreated.
