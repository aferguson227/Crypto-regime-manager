# V12 — Automatic Research Pipeline

V12 runs the pending one-variable diagnostic experiments automatically on every main workflow. It ranks results and publishes them on `docs/pipeline.html`. Production settings remain frozen and promotion still requires manual approval.
