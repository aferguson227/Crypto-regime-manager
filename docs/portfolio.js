const e=s=>String(s??'—').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const money=v=>v==null?'Unknown':new Intl.NumberFormat('en-GB',{style:'currency',currency:'USD'}).format(v);const pct=v=>v==null?'Unknown':`${v}%`;async function run(){const d=await(await fetch('portfolio_intelligence.json?t='+Date.now(),{cache:'no-store'})).json();document.querySelector('#summary').textContent=`${d.status} · generated ${d.generated_at}`;document.querySelector('#metrics').innerHTML=[['Portfolio health',pct(d.portfolio_health_score)],['Diversification',pct(d.diversification_score)],['Capital efficiency',pct(d.capital_efficiency_score)],['Allocated',money(d.allocated_capital)],['Reserved',money(d.reserved_capital)],['Deployable',money(d.deployable_capital)]].map(x=>`<div class="metric"><span>${e(x[0])}</span><strong>${e(x[1])}</strong></div>`).join('');document.querySelector('#priority').innerHTML=`<strong>${e(d.next_capital_priority||'No confirmed priority')}</strong><p class="muted">Advisory only. Manual approval remains required.</p>`;document.querySelector('#positions').innerHTML=(d.positions||[]).map(p=>`<article class="position"><header><strong>${e(p.bot_name)}</strong><span class="tag">#${e(p.deployable_priority)} ${e(p.action)}</span></header><p>${e(p.asset)} · ${p.active_deals} active deal(s) · ${e(p.overlap_group)}</p><p class="muted">Allocated ${e(money(p.allocated_capital))} · Reserve ${e(money(p.reserve_capital))} · Efficiency ${e(p.efficiency_score)}</p></article>`).join('')||'<p>No portfolio positions available.</p>';const groups=Object.entries(d.overlap_analysis||{}).map(([k,v])=>`<p><strong>${e(k)}</strong>: ${e((v.assets||[]).join(', '))}${v.concentration_warning?' <span class="warn">Concentration warning</span>':''}</p>`).join('');document.querySelector('#warnings').innerHTML=groups+(d.warnings||[]).map(x=>`<p class="warn">${e(x)}</p>`).join('')}run().catch(x=>document.querySelector('#summary').textContent=x.message);
