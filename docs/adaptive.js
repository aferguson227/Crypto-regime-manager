const esc=s=>String(s??'—').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const pct=v=>v==null?'Unknown':`${Number(v).toFixed(1)}%`;async function run(){const d=await(await fetch('adaptive_intelligence.json?t='+Date.now(),{cache:'no-store'})).json();document.querySelector('#summary').textContent=`${d.adaptation_enabled?'Adaptive evidence active':'Static model authoritative'} · generated ${d.generated_at}`;document.querySelector('#metrics').innerHTML=[['Adaptive status',d.adaptation_enabled?'Active (capped)':'Dormant'],['Minimum evidence',d.minimum_evidence_required],['Influence cap',pct(d.adaptive_influence_cap_pct)],['Next capital priority',d.next_capital_priority||'Unknown']].map(x=>`<div class="metric"><span>${esc(x[0])}</span><strong>${esc(x[1])}</strong></div>`).join('');document.querySelector('#strategies').innerHTML=(d.strategies||[]).map(s=>`<article class="position"><header><strong>#${s.rank} ${esc(s.bot_name)}</strong><span class="tag">${esc(s.action)}</span></header><p>${esc(s.asset)} · static ${pct(s.static_score)} · adaptive ${pct(s.adaptive_score)} · risk-adjusted ${pct(s.risk_adjusted_score)}</p><p class="muted">${esc(s.adaptation_status)} · ${s.evidence_count} judged outcomes · accuracy ${pct(s.observed_accuracy_pct)}</p>${(s.reasons||[]).map(x=>`<p>${esc(x)}</p>`).join('')}${(s.warnings||[]).map(x=>`<p class="warn">${esc(x)}</p>`).join('')}</article>`).join('')||'<p>No strategy records available.</p>';document.querySelector('#calibration').innerHTML=(d.confidence_calibration||[]).map(x=>`<p><strong>${esc(x.band)}</strong>: ${x.completed_and_judged||0} judged · observed ${pct(x.observed_accuracy_pct)} · error ${pct(x.calibration_error_pct)}</p>`).join('')||'<p>No completed outcomes available for calibration.</p>';document.querySelector('#warnings').innerHTML=(d.warnings||[]).map(x=>`<p class="warn">${esc(x)}</p>`).join('')||'<p>No warnings.</p>'}run().catch(x=>document.querySelector('#summary').textContent=x.message);
