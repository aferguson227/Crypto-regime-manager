# V15.1 Decision Preview Fix

- Replaces the mobile two-column metric grid with a responsive vertical decision list.
- Prevents horizontal overflow and clipped DCA text on narrow screens.
- Distinguishes `NOT APPLICABLE` from `BLOCK` when the current regime differs from the candidate's target regime.
- Shows current and target regimes separately.
- Keeps all execution, DCA and production safeguards unchanged.
